// Re-exported for import-path consistency with the other hooks in
// this folder. The actual implementation lives next to the provider
// so the context and the hook that reads it stay in sync.
export { useLanguage } from "@/lib/i18n/provider";
